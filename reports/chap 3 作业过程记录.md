### 作业要求
```rust
fn sys_trace(_trace_request: usize, _id: usize, _data: usize) -> isize
```
- 调用规范：
    - 这个系统调用有三种功能，根据 `trace_request` 的值不同，执行不同的操作：
    - 如果 `trace_request` 为 0，则 `id` 应被视作 `*const u8` ，表示读取当前任务 `id` 地址处一个字节的无符号整数值。此时应忽略 `data` 参数。返回值为 `id` 地址处的值。
```
- 把传进来的 `id` 当作一个 **用户空间地址**；
- 从这个地址读取一个字节的值（`u8` 类型）；
- 然后把这个字节的值返回给调用者。
```
    - 如果 `trace_request` 为 1，则 `id` 应被视作 `*mut u8` ，表示写入 `data` （作为 `u8`，即只考虑最低位的一个字节）到该用户程序 `id` 地址处。返回值应为0。
    - 如果 `trace_request` 为 2，表示查询当前任务调用编号为 `id` 的系统调用的次数，返回值为这个调用次数。**本次调用也计入统计** 。
    - 否则，忽略其他参数，返回值为 -1。

- 你可能会注意到，这个调用的读写并不安全，使用不当可能导致崩溃。这是因为在下一章节实现地址空间之前，系统中缺乏隔离机制。所以我们 **不要求你实现安全检查机制，只需通过测试用例即可** 。
- 你还可能注意到，这个系统调用读写本任务内存的功能并不是很有用。这是因为作业的灵感来源 syscall 主要依靠 trace 功能追踪其他任务的信息，但在本章节我们还没有进程、线程等概念，所以简化了操作，只要求追踪自身的信息。
提示:
- 系统调用次数可以考虑在内核态的 `syscall` 函数中统计。
- 可以扩展 `TaskManagerInner` 中的结构来维护新的信息。
### TODO
- [x] 定义读取和写入syscall计数的接口
- [x] 定义TCB中计数的数据结构
- [x] 实现函数
- [x] 完成数据结构初始化
## 一些思考
task1调用trace(2,id,0)那么这时候会陷入trap然后进入TrapHandler调用syscall.进入syscall分发到trace.也就是说如果我能定义一个全局变量计数让trace和syscall都能访问,然后syscall来增加,trace用来读取. 但是现在问题来了,问题就是怎么按照任务来区分呢? trace里无法得知调用他的任务是哪个

- **你现在的内核里，谁知道“当前正在运行的是哪个任务”？**
    - 你有没有一个“当前任务索引/句柄”的全局来源？（比如 TaskManager 里保存 `current`，或者某个 per-hart 变量，或者调度器返回的当前 TCB 引用。）:有TaskManager和TaskManagerInner
    - 这个信息在 trap 进入、执行 syscall 分发时是否**总是可用**？:按道理也是可以,我觉得可以用TaskControlBlock里面创建一个==计数结构==,然后syscall通过访问当前任务的TCB来增加技术
- **syscall 分发入口如何“拿到当前任务的 TCB（或其内部计数结构）”？**
    - 你是否已经有对 `TaskManager` 的只/可变访问路径？（比如 `TASK_MANAGER.inner_exclusive_access()` 之类的机制。）
    - 如果没有直拿 TCB 的接口，你是打算：  
        A. 在 `TaskManagerInner` 里新增“按任务下标返回可变引用”的方法？  
        B. 还是在更外层缓存“当前任务 id”，用它去索引一个“按任务维度”的计数存储？
        
- **计数的存储结构放哪一层，粒度怎么定？**
    - 你前面说“放 TCB 里”——那是“每个任务一个**定长数组**（按 syscall 号索引）”，还是一个**稀疏结构**（Map）？
    - 你是否已经确定“系统调用号的上界”，从而能用数组？如果没有上界，如何避免越界？
    - 如果用全局数组 + “当前任务 id”做第一维、syscall 号做第二维，和“放在 TCB 内部”相比，有什么并发/一致性或生命周期管理差异？
        
- **“本次也计入”的时序你打算怎么保证？**
    - 你准备在分发入口**先** `++`，再调用具体处理（包括 `sys_trace`），还是相反？
    - 当 `trace_request == 2` 时，你预期的返回值应该**包含**这一次进入内核的计数，对吗？那你的 `++` 和“读出次数”的先后顺序如何安排才能做到这一点？
- **中断与一致性（Lab1 是否有影响）**
    - 你的 syscall 分发期间，**时钟中断可能发生**并抢占吗？如果有，出现“计数自增了一半被切走”的可能吗？
    - 在 Lab1 的假设下你需要不要专门关中断/进入临界区？如果不需要，你给出的理由是什么？（单核 + 调度点只在明确位置触发？）
- **读与写的职责分离**
    - 你说“syscall 入口自增，`sys_trace(2, …)` 只读取”——那读取路径如何拿到**同一份**每任务计数数据？
    - 你是否要为“读计数”暴露一个内部查询 API（例如 `TaskManager` 提供 `get_count(task_id, syscall_id)`），还是 `sys_trace` 自己直达当前 TCB？
    - 这两种做法哪种更符合你当前的模块边界与可测试性？


### “当前任务是谁”从哪里拿？

- 你的 `add_current_count`/`read_current_count` 里写了 `//get the current task number`。
    
    - 你现在的内核里**谁持有当前任务索引/句柄**？（例如 `TaskManagerInner.current`？还是调度器返回的 TCB 引用？）
        
    - 在**陷入 `syscall` 分发入口**时，这个“当前任务”的标识是否**总是可用**？
        
    - 你准备在 `TaskManager` 层提供一个**拿“当前 TCB 可变引用”**的 API，还是只暴露“当前任务 id”，再用它去索引数据结构？

### “本次也计入”的时序正确吗？

- 你在 `syscall(...)` 里先 `add_current_count(syscall_id)`，再分发到 `sys_trace`。这意味着：
    
    - 当用户执行 `sys_trace(2, X, 0)`，你返回的 `X` 的计数**应该已经包含本次**。
        
    - 但请自问：你在 `sys_trace(2, X, …)` 分支里**只是读取**，有没有任何路径可能让“先读后加”？（比如异常返回、早退路径）
        
- 另外：**如果 `syscall_id` 本身非法**（比如你的内核不认识这个号），你计划**还要加 1 吗**？规范没写，但你需要**自洽**，否则统计会出现“看不懂”的差异。

### 3) 计数结构的形状与上界

- 你说要把计数放进 TCB。那你准备是：
    
    - **定长数组**：`counts[MAX_SYSCALL_ID+1]` 按系统调用号索引？
        
    - 还是**映射**（稀疏存储）？
        
- 你是否已经有“**系统调用号上界**”这一常量？如果没有，上界不明时怎么避免越界？
    
- 注意：题目说不做**内存读写安全检查**，但**数组越界**会直接**内核崩溃**，这和题目“只需通过测例”是矛盾的。你要不要在 `trace(2, id, …)` 的 `id` 上**自行做边界策略**（返回 -1？忽略？还是截断到上界）？你的选择会影响测例。

### 5) 与调度/中断的一致性

- Lab1 下**时钟中断**可能打断 `syscall` 执行吗？如果可以：
    
    - 你在给当前任务计数 `+1` 的瞬间被切走，会不会留下**部分更新**？
        
    - 你访问 `TaskManager` 和当前 TCB 的路径是否**天然互斥**（例如独占借用/锁语义）？
        
    - 如果你依赖“单核+明确调度点”，请明确写下来你依赖的假设。

### 回答GPT的提问
1. `add_current_count(&self, …)` 的**可变性**问题：现在这签名无法修改内部计数: 这一点不用担心,因为我没是用只读变量里的InnerCell(以常量包变量)
2. “当前任务是谁”的**可达路径**：需要一个明确、可用且不会打破借用规则的拿法:`TaskManagerInner.current`就是索引
3. 数据结构我还没想好,我倒是想用映射,不同syscall id不一样,而且如果一个id号很大会占用内存,并且数组的效率也不比映射高
4. 目前来说S mode下syscall过程中不会被打断

```
add_current_count(id)  -> 调用 TASK_MANAGER.add_current_count(id)
read_current_count(id) -> 调用 TASK_MANAGER.read_current_count(id)
```